Gem::Specification.new do |s|
  s.name        = "not_so_slim_gem"
  s.version     = "0.0.0"
  s.summary     = "A large gem that doesn't do much"
  s.description = "A large gem that doesn't do much"
  s.authors     = ["Tim Downey"]
  s.email       = "tim@downey.dev"
  s.files       = ["lib/not_so_slim_gem.rb"]
  s.license       = "MIT"
end
